{
  "name": "pragmaticfox-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler dev",
    "validate": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run",
    "deploy": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
